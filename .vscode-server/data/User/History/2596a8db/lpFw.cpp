#include "minFunc.h"
const int * min(const int arr[], int arrSize) {
    if ( arrSize == 0 ){
      return nullptr;
    }
    if (arrSize == 1){
        return 
    }

    string r = s.substr( 1, s.size() - 2 );

   flipString( r );

    
}